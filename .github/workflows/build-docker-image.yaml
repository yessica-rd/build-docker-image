name: Build docker image

on:
  push:
    branches:
      - test/claasp-with-dockerhub-image-2

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Cache Docker layers
        uses: actions/cache@v3
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      - name: Build
        uses: docker/build-push-action@v4
        id: built-image
        with:
          context: ./docker/Dockerfile
          push: false
          load: true
          tags: claasp-lib:latest
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache-new,mode=max

      - name: Move cache
        run: |
          rm -rf /tmp/.buildx-cache
          mv /tmp/.buildx-cache-new /tmp/.buildx-cache

      - name: Run pytests
        run: docker run --rm -v $PWD:/home/sage/tii-claasp claasp-lib:latest make remote-pyt

    
    # - name: Checkout
    #   uses: actions/checkout@v4
    #   with:
    #     fetch-depth: 0
    
    # - name: Docker login
    #   env:
    #     DOCKER_USER: ${{ secrets.DOCKER_USER }}
    #     DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
    #   run: |
    #     docker login -u $DOCKER_USER -p $DOCKER_PASSWORD

    # - name: Build Docker Image
    #   run: |
    #     docker build -t yessicard/docker-claasp-testing:latest ./docker/

    # - name: Push Docker Image
    #   run: |
    #     docker push yessicard/docker-claasp-testing:latest

    # - name: Run pytests
    #   run: docker run --rm -v $PWD:/home/sage/tii-claasp yessicard/claasp:latest make remote-pytest
